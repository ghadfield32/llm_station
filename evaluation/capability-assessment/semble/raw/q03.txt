{"query": "where does validation enforce that L3 and L4 risk tiers require approval", "results": [{"chunk": {"content": "# ---- judges.yaml ------------------------------------------------------------\nclass JudgeSpec(Strict):\n    name: str\n    role_alias: str                       # which LiteLLM role this judge calls\n    blocks_on: list[Decision] = [Decision.BLOCK]\n    escalation_role: str | None = None    # cross-provider escalation alias\n    max_cost_usd: float = Field(default=0.10, ge=0)\n    timeout_seconds: int = Field(default=120, ge=1)\n\n\nclass JudgeStage(Strict):\n    stage: str\n    cheap_first: bool = True\n    judges: list[JudgeSpec]\n\n\nclass JudgeConfig(Strict):\n    schema_version: str\n    # judge that WRITES must be reviewed by a different provider family\n    cross_provider_review: bool = True\n    stages: list[JudgeStage]\n\n\n# ---- gates.yaml -------------------------------------------------------------\nclass TierPolicy(Strict):\n    auto: bool\n    requires_approval: bool\n    required_stages: list[str]\n    forbidden_auto_actions: list[str] = []\n\n\nclass GatesConfig(Strict):\n    schema_version: str\n    tiers: dict[RiskTier, TierPolicy]\n\n    @model_validator(mode=\"after\")\n    def _all_tiers(self):\n        missing = set(RiskTier) - set(self.tiers)\n        if missing:\n            raise ValueError(f\"gates.yaml missing tiers: {sorted(m.value for m in missing)}\")\n        # L3/L4 must require approval \u2014 hard safety invariant\n        for t in (RiskTier.L3, RiskTier.L4):\n            if not self.tiers[t].requires_approval:\n                raise ValueError(f\"{t.value} must require approval\")\n        return self", "file_path": "schemas\\contracts.py", "start_line": 103, "end_line": 147, "language": "python", "location": "schemas\\contracts.py:103-147"}, "score": 0.0191214581925377}, {"chunk": {"content": "if tier in (RiskTier.L3, RiskTier.L4) and not policy.requires_approval:\n        print(\"  INVARIANT VIOLATION: L3/L4 without approval\"); ok = False\n\n    print(\"dry-run: PASS\" if ok else \"dry-run: FAIL\")\n    return 0 if ok else 1\n\n\nif __name__ == \"__main__\":\n    sys.exit(main())", "file_path": "scripts\\smoke_mission.py", "start_line": 48, "end_line": 56, "language": "python", "location": "scripts\\smoke_mission.py:48-56"}, "score": 0.018907504153405794}, {"chunk": {"content": "schema_version: command-center.gates.v1\n\n# Risk tiers. Hard invariant (enforced by the contract): L3/L4 must require approval.\ntiers:\n  L0_read_only:\n    auto: true\n    requires_approval: false\n    required_stages: [intake]\n  L1_plan_only:\n    auto: true\n    requires_approval: false\n    required_stages: [intake, plan]\n  L2_local_edits:\n    auto: true\n    requires_approval: false\n    required_stages: [intake, plan, pre-commit]\n  L3_external_write:\n    auto: false\n    requires_approval: true\n    required_stages: [pre-push]\n  L4_dangerous:\n    auto: false\n    requires_approval: true\n    required_stages: [architecture, pre-push]\n    forbidden_auto_actions: [merge, deploy, publish_package, rotate_secrets, terraform_apply, delete_data]\n", "file_path": "configs\\gates.yaml", "start_line": 1, "end_line": 25, "language": "yaml", "location": "configs\\gates.yaml:1-25"}, "score": 0.01728379314055252}, {"chunk": {"content": "## 2. The contract model \u2014 the one rule\n\nThe whole system obeys one rule, and that rule is what makes it hard to break:\n\n```\nconfigs/*.yaml      = the editable source of truth\nschemas/*.py        = Pydantic contracts that validate it\ngenerated/*         = disposable rendered output (never hand-edited)\nledger.sqlite       = the only runtime state\nMakefile / cc.ps1   = the only operator interface\n.env                = secrets (never in YAML, never committed)\n```\n\nEdit YAML \u2192 `make validate` \u2192 `make render` \u2192 run a target. Nothing runs until\nvalidate passes; nothing external happens until the Ledger and gates approve\nit; nothing reaches a main branch without GitHub enforcing it.\n\n**Proven, not asserted** \u2014 these bad edits are rejected by `make validate`\nbefore they can ship (all tested):\n\n- a typo'd key (`priorty:`) \u2192 rejected (`extra=\"forbid\"`)\n- two models with the same priority in one role \u2192 rejected\n- two canaries in one role, or `canary_weight` outside 0\u20131 \u2192 rejected\n- a missing risk tier, or **L3/L4 without `requires_approval`** \u2192 rejected\n- a `repo_task` environment that is persistent or holds secrets \u2192 rejected\n- a provider (OpenAI/Anthropic/OpenRouter) route in a LiteLLM role \u2192 rejected\n- a scheduled check proposing L3/L4, `on_fail: auto_fix`, stewardship above\n  L2, a check with no owner/schedule/evidence \u2192 rejected (eight cases firing)\n- `scout.propose_only: false` \u2192 rejected (\"swap to the leaderboard top\" is\n  not representable)\n\n", "file_path": "docs\\MASTER.md", "start_line": 102, "end_line": 132, "language": "markdown", "location": "docs\\MASTER.md:102-132"}, "score": 0.016929484001721392}, {"chunk": {"content": "def main() -> int:\n    evals = EvalsConfig.model_validate(yaml.safe_load(open(\"configs/evals.yaml\")))\n    gates = GatesConfig.model_validate(yaml.safe_load(open(\"configs/gates.yaml\")))\n    tiers = gates.tiers\n\n    ok = True\n    for c in evals.cases:\n        tier = tiers.get(c.expected_risk.value)\n        if tier is None:\n            print(f\"  FAIL {c.name}: expected_risk {c.expected_risk} not in gates.yaml\"); ok = False; continue\n        # L3/L4 cases must land on a non-auto, approval-required tier\n        if c.expected_risk.value in (\"L3_external_write\", \"L4_dangerous\"):\n            if tier.auto or not tier.requires_approval:\n                print(f\"  FAIL {c.name}: {c.expected_risk} tier must be non-auto + approval-required\"); ok = False; continue\n            if c.expected_auto_allowed is True:\n                print(f\"  FAIL {c.name}: asserts auto allowed for a gated tier\"); ok = False; continue\n        # forbidden stages must not be in the tier's required stages\n        bad = set(c.forbidden_stages) & set(tier.required_stages)\n        if bad:\n            print(f\"  FAIL {c.name}: forbids stage(s) {sorted(bad)} that the tier requires\"); ok = False; continue\n        print(f\"  OK   {c.name}  ({c.expected_risk.value})\")\n    print(\"evals: PASS\" if ok else \"evals: FAIL\")\n    return 0 if ok else 1\n\nif __name__ == \"__main__\":\n    sys.exit(main())", "file_path": "scripts\\run_evals.py", "start_line": 18, "end_line": 43, "language": "python", "location": "scripts\\run_evals.py:18-43"}, "score": 0.016554816831223094}]}
