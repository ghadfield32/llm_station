{"query": "how are the LiteLLM virtual keys minted on Windows", "results": [{"chunk": {"content": "### Phase 1 \u2014 control plane (first-boot sequence)\n\n```bash\n# on the VPS, after Docker + Tailscale are up (Windows: .\\scripts\\cc.ps1 <target>)\ngit clone <this repo> && cd <repo>\nmake setup          # deps, .env, validate, build images\n# edit .env: confirm OLLAMA_API_BASE; do NOT add provider API keys\nmake verify-base    # FAILS on placeholder digest / missing first-boot secrets\nmake bootstrap      # FIRST BOOT: litellm + db + ledger only, waits for health\nmake keys           # mints the 2 virtual keys \u2192 paste BOTH into .env\nmake verify         # full runtime prerequisites, including the virtual keys\nmake up             # verify \u2192 render \u2192 full stack\nmake health         # all OK?\nmake mission-dryrun # fake L0\u2013L4 missions through gates+judges, no model calls\nmake live-smoke     # real local replies through Ollama + LiteLLM aliases\n```\n\nWhy `bootstrap` before `up`: clients read their LiteLLM key from `.env`, but\nthat key doesn't exist until LiteLLM is up and `make keys` mints it.\n`bootstrap` starts just the infra so keys can be minted first.\n\nThe live smoke proves: Ollama direct reply \u00b7 LiteLLM `triage`/`planner`/\n`local-judge` aliases \u00b7 `gpt-*` and `claude-*` names **denied** through\nLiteLLM \u00b7 executor shell has no provider keys \u00b7 forbidden-providers check\npasses. No skip-Ollama path exists \u2014 Ollama is required; calls fail closed.\n\n**Done when:** live smoke passes; a channel can open a Ledger mission and an\nL3 request shows `awaiting_approval`.\n\n", "file_path": "docs\\MASTER.md", "start_line": 504, "end_line": 532, "language": "markdown", "location": "docs\\MASTER.md:504-532"}, "score": 0.019201228878648235}, {"chunk": {"content": "## Phase 1 \u2014 Control plane (always-on brain)\nGoal: LiteLLM + Ledger + Judge Gate + monitoring, with Hermes added only after its\nruntime is verified.\n\n```bash\n# on the VPS, after Tailscale + Docker are installed\ncd ~/command-center\nmake setup        # checks docker, creates .env, validates, builds local services\n# --- edit .env: confirm OLLAMA_API_BASE and optional GITHUB_TOKEN.\n#     Do not add OpenAI/Anthropic/OpenRouter API keys.\n#     Also replace the placeholder digest in docker-compose.yml + Makefile\n#     with a real verified LiteLLM image digest (mandatory). ---\nmake verify-base  # confirms first-boot prerequisites before virtual keys exist\nmake bootstrap    # FIRST BOOT: brings up litellm+db+ledger only, waits until healthy\nmake keys         # mints the 2 virtual keys -> paste BOTH into .env (confirm non-empty)\nmake verify       # confirms full runtime prerequisites, including virtual keys\nmake up           # verify + render + default stack; Hermes is behind an optional profile\nmake health       # all OK?\nmake live-smoke     # proves Ollama direct + LiteLLM local aliases + executor-auth\n```\nAfter you choose a verified Hermes runtime, point it at LiteLLM using the\nOpenAI-compatible base URL and `HERMES_LITELLM_KEY`, then connect the first channel\nsuch as Telegram. Set up Uptime Kuma monitors and `make backup` on a schedule with a\nmonthly `make restore-drill`.\n", "file_path": "docs\\runbook.md", "start_line": 19, "end_line": 42, "language": "markdown", "location": "docs\\runbook.md:19-42"}, "score": 0.016460552941685254}, {"chunk": {"content": "# SETUP-REMAINING - local-only command center\n\nState as of June 12, 2026: local repo validation is green, LiteLLM is pinned by\ndigest, Ollama is reachable from both Windows and Docker, required local models are\ninstalled, LiteLLM virtual keys are minted, health checks pass, and live smoke prints\nreal local responses through Ollama and LiteLLM. Hermes is intentionally optional in\nCompose until a real runtime image or local Hermes bridge is selected.\n\n---\n\n## 1. Correct Cost Routing\n\n| Lane | What runs here | Provider API charge |\n| --- | --- | ---: |\n| Claude Code executor | Coding missions in leased worktrees, authenticated by Claude login | $0 API charge |\n| Codex executor | Fallback coding missions, authenticated by ChatGPT login | $0 API charge |\n| LiteLLM local gateway | Hermes, Judge Gate, triage, planner, local judges | $0 API charge |\n| Ollama on 4090/host | The actual model runtime behind LiteLLM | $0 API charge |\n\nDo not create or store OpenAI, Anthropic, or OpenRouter API keys for this architecture.\nProvider keys are forbidden by validation.\n\n`HERMES_LITELLM_KEY` is generated by your own LiteLLM server. It is a virtual access key\nthat allows Hermes to call approved local aliases such as `triage`, `planner`, and\n`local-judge`. Creating it costs no API money.\n\n---\n\n", "file_path": "docs\\SETUP-REMAINING.md", "start_line": 1, "end_line": 28, "language": "markdown", "location": "docs\\SETUP-REMAINING.md:1-28"}, "score": 0.016321911756507972}, {"chunk": {"content": "## 8. First-boot build flow \u2014 the order that actually works\n\n```mermaid\nflowchart TD\n    S1[\"make setup<br/>deps \u00b7 .env \u00b7 validate \u00b7 build images\"] --> S2[\"verify pinned LiteLLM digest<br/>update only during upgrades\"]\n    S2 --> S3[\"make bootstrap<br/>litellm-db + litellm + ledger ONLY\"]\n    S3 --> S4[\"make keys<br/>mint 2 virtual keys\"]\n    S4 --> S5[\"\u270b paste keys into .env<br/>HERMES_LITELLM_KEY \u00b7 JUDGE_GATE_LITELLM_KEY\"]\n    S5 --> S6[\"make up<br/>verify \u2192 render \u2192 full stack\"]\n    S6 --> S7[\"make health\"]\n    S7 --> S8[\"make mission-dryrun<br/>L0\u2013L4, no real model calls\"]\n\n    classDef manual fill:#4a3a1e,stroke:#d9a534,color:#fff\n    class S2,S5 manual\n```\n\n**Why `bootstrap` before `up`:** hermes reads its model key from `.env`, but that key doesn't exist until LiteLLM is running and `make keys` mints it. `bootstrap` starts just the infra so you can mint keys first; `up` then brings up the full stack with keys in place. The two amber steps are the only manual ones.\n\n---\n\n", "file_path": "docs\\visuals.md", "start_line": 232, "end_line": 251, "language": "markdown", "location": "docs\\visuals.md:232-251"}, "score": 0.01592174412209454}, {"chunk": {"content": "## Fix 3 \u2014 local Ollama model now reachable\n- Added `OLLAMA_API_BASE` env var to the `litellm` service in docker-compose.yml.\n  Without it, VPS LiteLLM looked for Ollama on its own localhost and every\n  triage/local-judge call silently fell back to paid models.\n- Added `OLLAMA_API_BASE=http://REPLACE-WITH-4090-TAILSCALE-IP:11434` to .env.example.\n  Set this to the 4090's Tailscale IP in Phase 2.\n\n## Still on you (can't be mechanically fixed)\n- Replace the LiteLLM digest in docker-compose.yml with one you pull + verify.\n- Add real `ANTHROPIC_API_KEY` and `OPENAI_API_KEY` before `make bootstrap`.\n- Run `make bootstrap`, then `make keys`, then paste the minted `HERMES_LITELLM_KEY`\n  and `JUDGE_GATE_LITELLM_KEY` into `.env` before `make up`.\n- Confirm Hermes v0.15.x env-var names (OPENAI_BASE_URL / OPENAI_API_KEY /\n  HERMES_DEFAULT_MODEL) against current docs at install \u2014 they're assumed.\n- Set OLLAMA_API_BASE to the real 4090 Tailscale IP when you reach Phase 2.\n- Optional but expected for the full system: `OPENROUTER_API_KEY` for hosted-open\n  fallback and `GITHUB_TOKEN` for L3 push/PR automation.\n\nCurrent verifier behavior:\n- `make verify-base` blocks on placeholder digest plus missing Anthropic/OpenAI keys.\n- `make verify` additionally blocks on missing Hermes/Judge Gate LiteLLM virtual keys.\n- Placeholder OpenRouter, GitHub, and Ollama values warn instead of blocking because\n  those are Phase 2/optional capabilities.\n", "file_path": "PREFLIGHT-FIXES.md", "start_line": 22, "end_line": 44, "language": "markdown", "location": "PREFLIGHT-FIXES.md:22-44"}, "score": 0.015803472379248482}]}
