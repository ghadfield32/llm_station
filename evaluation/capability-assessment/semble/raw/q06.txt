{"query": "which script enforces that no provider API keys exist", "results": [{"chunk": {"content": "#!/usr/bin/env python3\n\"\"\"Fail if cloud provider routes or provider API keys re-enter the local-only setup.\"\"\"\n\nfrom __future__ import annotations\n\nimport os\nimport sys\nfrom pathlib import Path\nfrom typing import Any\n\nimport yaml\n\n\nROOT = Path(__file__).resolve().parents[1]\nFORBIDDEN_KEYS = {\"ANTHROPIC_API_KEY\", \"OPENAI_API_KEY\", \"OPENROUTER_API_KEY\"}\nFORBIDDEN_MODEL_FRAGMENTS = (\"openai/\", \"anthropic/\", \"openrouter/\", \"gpt-\", \"claude-\")\n\n\ndef dotenv_keys(path: Path) -> set[str]:\n    if not path.exists():\n        return set()\n    keys: set[str] = set()\n    for raw in path.read_text(encoding=\"utf-8\").splitlines():\n        line = raw.strip()\n        if not line or line.startswith(\"#\") or \"=\" not in line:\n            continue\n        keys.add(line.split(\"=\", 1)[0].strip())\n    return keys\n\n\ndef check_env_files(errors: list[str]) -> None:\n    for rel in (\".env\", \".env.example\"):\n        keys = dotenv_keys(ROOT / rel)\n        leaked = sorted(keys & FORBIDDEN_KEYS)\n        if leaked:\n            errors.append(f\"{rel} defines forbidden provider key(s): {', '.join(leaked)}\")\n\n\ndef check_process_env(errors: list[str]) -> None:\n    leaked = sorted(key for key in FORBIDDEN_KEYS if os.environ.get(key))\n    if leaked:\n        errors.append(f\"process environment contains forbidden provider key(s): {', '.join(leaked)}\")", "file_path": "scripts\\check_forbidden_providers.py", "start_line": 1, "end_line": 42, "language": "python", "location": "scripts\\check_forbidden_providers.py:1-42"}, "score": 0.02112231697985641}, {"chunk": {"content": "## 2. Remaining Local Items\n\n- [x] Replace `REPLACE_WITH_VERIFIED_DIGEST` in `docker-compose.yml` and `Makefile`.\n- [x] Confirm `.env` contains no provider API keys.\n- [x] Confirm `OLLAMA_API_BASE` points to reachable Ollama:\n      `http://host.docker.internal:11434` locally, or the 4090 Tailscale URL from VPS.\n- [x] Confirm `LEDGER_HOST_PORT=8091` unless your host already has that port allocated.\n- [x] Run bootstrap and mint virtual keys:\n\n```powershell\n.\\scripts\\cc.ps1 verify-base\n.\\scripts\\cc.ps1 bootstrap\n.\\scripts\\cc.ps1 keys\n.\\scripts\\cc.ps1 verify\n.\\scripts\\cc.ps1 up\n.\\scripts\\cc.ps1 health\n.\\scripts\\cc.ps1 live-smoke\n```\n\n`cc.ps1 keys` must only require `LITELLM_MASTER_KEY`, Postgres, Ledger, and a running\nLiteLLM proxy. It must not require provider API keys.\n\n---\n\n", "file_path": "docs\\SETUP-REMAINING.md", "start_line": 29, "end_line": 52, "language": "markdown", "location": "docs\\SETUP-REMAINING.md:29-52"}, "score": 0.01721801716896347}, {"chunk": {"content": "Param(\n  [string]$TriageAlias = \"triage\",\n  [string]$PlannerAlias = \"planner\",\n  [string]$JudgeAlias = \"local-judge\",\n  [string]$LiteLLMUrl = $env:LITELLM_URL\n)\n\n$ErrorActionPreference = \"Stop\"\nSet-Location (Split-Path -Parent $PSScriptRoot)\n\nif (-not $LiteLLMUrl) {\n  $LiteLLMUrl = \"http://localhost:4000\"\n}\n\nfunction Fail-IfProviderEnvExists {\n  $Forbidden = @(\"OPENAI_API_KEY\", \"ANTHROPIC_API_KEY\", \"OPENROUTER_API_KEY\")\n  foreach ($Key in $Forbidden) {\n    if (Test-Path \"Env:$Key\") {\n      Write-Host \"$Key must not exist in the executor process environment. [FAIL]\"\n      exit 1\n    }\n    if ([Environment]::GetEnvironmentVariable($Key, \"User\")) {\n      Write-Host \"$Key must not exist in User environment variables. [FAIL]\"\n      exit 1\n    }\n    if ([Environment]::GetEnvironmentVariable($Key, \"Machine\")) {\n      Write-Host \"$Key must not exist in Machine environment variables. [FAIL]\"\n      exit 1\n    }\n  }\n}\n\nfunction Read-DotEnvValue {\n  param([Parameter(Mandatory = $true)][string]$Name)\n  if (-not (Test-Path -LiteralPath \".env\")) {\n    return \"\"\n  }\n  foreach ($Line in Get-Content -LiteralPath \".env\") {\n    $Trimmed = $Line.Trim()\n    if (-not $Trimmed -or $Trimmed.StartsWith(\"#\") -or -not $Trimmed.Contains(\"=\")) {\n      continue\n    }\n    $Parts = $Trimmed.Split(\"=\", 2)\n    if ($Parts[0] -eq $Name) {\n      return $Parts[1]\n    }\n  }\n  return \"\"\n}", "file_path": "scripts\\live_smoke.ps1", "start_line": 1, "end_line": 49, "language": "powershell", "location": "scripts\\live_smoke.ps1:1-49"}, "score": 0.016125509770452948}, {"chunk": {"content": "if [[ -n \"$selected_secret_file\" ]]; then\n        print_info \"Processing private secret file...\"\n        \n        # Extract keys from secret file\n        local secret_keys=()\n        while IFS= read -r key; do\n            if [[ -n \"$key\" ]]; then\n                secret_keys+=(\"$key\")\n            fi\n        done < <(extract_keys_from_file \"$selected_secret_file\" | tr ' ' '\\n')\n        \n        if [[ ${#secret_keys[@]} -eq 0 ]]; then\n            print_warning \"No valid environment variables found in private secret file.\"\n        else\n            print_info \"Found ${#secret_keys[@]} keys in private secret file: ${secret_keys[*]}\"\n            \n            # Create a temporary file for processing\n            local temp_file=$(mktemp)\n            cp \"$output_path\" \"$temp_file\"\n            \n            # Replace keys that exist in both files\n            local replaced_count=0\n            while IFS= read -r line; do\n                # Skip comments and empty lines in secret file\n                if [[ \"$line\" =~ ^[[:space:]]*# ]] || [[ \"$line\" =~ ^[[:space:]]*$ ]]; then\n                    continue\n                fi\n                \n                # Extract key-value pair from secret file", "file_path": "appflowy_kanban\\AppFlowy-Cloud\\script\\generate_env.sh", "start_line": 267, "end_line": 295, "language": "bash", "location": "appflowy_kanban\\AppFlowy-Cloud\\script\\generate_env.sh:267-295"}, "score": 0.011349741850891186}, {"chunk": {"content": "}\n\nswitch ($Task.ToLowerInvariant()) {\n  \"help\" { Show-Help }\n  \"init-env\" { Invoke-Python \"scripts/init_env.py\" }\n  \"validate\" { Invoke-Python \"scripts/validate_config.py\" }\n  \"cross-refs\" { Invoke-Python \"scripts/check_cross_refs.py\" }\n  \"forbidden-providers\" { Invoke-Python \"scripts/check_forbidden_providers.py\" }\n  \"schema\" { Invoke-Python \"scripts/render_json_schema.py\" }\n  \"render\" {\n    Invoke-Python \"scripts/validate_config.py\"\n    Invoke-Python \"registry/render.py\"\n  }\n  \"evals\" { Invoke-Python \"scripts/run_evals.py\" }\n  \"verify-base\" { Invoke-Python \"scripts/verify_env.py\" \"--mode\" \"base\" }\n  \"verify\" { Invoke-Python \"scripts/verify_env.py\" \"--mode\" \"full\" }\n  \"keys\" {", "file_path": "scripts\\cc.ps1", "start_line": 70, "end_line": 86, "language": "powershell", "location": "scripts\\cc.ps1:70-86"}, "score": 0.011141288587246003}]}
