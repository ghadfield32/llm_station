"""Command Center MCP servers (read-only, typed, allowlisted).

Currently: the Life Center status gateway (see ``life_center_status``). Each
server wraps only pre-declared, no-free-form-arg functions and never exposes a
generic action/command surface.
"""
